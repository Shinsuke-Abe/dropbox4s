box: amutake/activator@0.0.2
steps:
  - wercker/mktemp@1.0.0
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - mktemp:
        envvar: downloadRoot
        directory: true
    - script:
        name: run test
        code: |
          activator test